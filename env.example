# Environment configuration for WhereInTheWorld
# Copy to .env and fill in values. NEVER commit .env to version control.

# ── Database ──
WITW_DB_PASSWORD=
WITW_DB_HOST=whereintheworld-db
WITW_DB_PORT=5432
WITW_DB_USER=witw
WITW_DB_NAME=whereintheworld

# ── API Auth ──
WITW_API_KEY=

# ── Tailscale (optional — for serve docs) ──
TAILSCALE_HOSTNAME=
TAILSCALE_DOMAIN=
